import { allowMethods, db, handleError, json, requireAdmin, requireUser } from './_lib.js';
export default async function handler(req,res){if(!allowMethods(req,res,['GET']))return;try{const user=await requireUser(req);requireAdmin(user);const supabase=db();const [{data:payments,error:pErr},{count:pending,error:aErr}]=await Promise.all([supabase.from('payments').select('amount_pi,purpose,status,created_at').eq('status','completed').order('created_at',{ascending:false}),supabase.from('apps').select('*',{count:'exact',head:true}).eq('status','pending')]);if(pErr||aErr)throw pErr||aErr;const total=(payments||[]).reduce((s,p)=>s+Number(p.amount_pi||0),0);return json(res,200,{totalRevenuePi:total,pendingApps:pending||0,payments:payments||[]})}catch(e){return handleError(e,res,'Unable to load admin stats')}}
