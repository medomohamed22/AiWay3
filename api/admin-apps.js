import { allowMethods, db, handleError, json, requireAdmin, requireUser, cleanText } from './_lib.js';
export default async function handler(req,res){if(!allowMethods(req,res,['GET','PATCH']))return;try{const user=await requireUser(req);requireAdmin(user);const supabase=db();if(req.method==='GET'){const {data,error}=await supabase.from('apps').select('*,users!apps_owner_id_fkey(username)').order('created_at',{ascending:false});if(error)throw error;return json(res,200,{apps:data||[]})}const {id,status,isFeatured,adminNote}=req.body||{};if(!id||!['pending','published','rejected','suspended'].includes(status))return json(res,400,{error:'Invalid review action'});const patch={status,admin_note:cleanText(adminNote,500),is_featured:Boolean(isFeatured),published_at:status==='published'?new Date().toISOString():null};const {data,error}=await supabase.from('apps').update(patch).eq('id',id).select('*').single();if(error)throw error;return json(res,200,{app:data})}catch(e){return handleError(e,res,'Unable to review apps')}}
