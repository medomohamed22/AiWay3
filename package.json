{
  "name": "nano-banana-ai-suite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": { "node": ">=20" },
  "devDependencies": { "vercel": "latest" }
}
